# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
ENV/
env/
*.egg-info/
dist/
build/

# Node
node_modules/
npm-debug.log
yarn-error.log
dist/

# Databases
*.db
*.sqlite
*.sqlite3
*.duckdb
*.duckdb.wal

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# App
.env
.env.local
# real credentials live ONLY in the unit file on the VPS; the repo carries the
# placeholder template (deploy/optionslab.service) â€” never put real creds there
optionslab.service
!deploy/optionslab.service
dhan_config.json
scrip_master_mcx.csv

# generated / packaged artifacts
app/static/
optionslab-deploy.zip
.pytest_cache/
.claude/

# dead parallel UI (frontend/ is canonical)
ui/

# private keys must never enter git
*.key
*.pem

# local deploy target (real IP/key path) — never commit
deploy/push.local.ps1

# research findings stay local (repo is public)
docs/observations.md
